import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Main brand colors
  static const Color primary = Color(0xFF079A92);
  static const Color primaryDark = Color(0xFF007D77);
  static const Color primaryLight = Color(0xFFDDF7F5);

  // Secondary colors
  static const Color secondary = Color(0xFF55CEC7);
  static const Color secondaryLight = Color(0xFFAEEAE6);

  // Background
  static const Color background = Color(0xFFF8FFFF);
  static const Color surface = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF173B39);
  static const Color textSecondary = Color(0xFF6F8584);
  static const Color textLight = Color(0xFF9BAEAD);

  // Borders
  static const Color border = Color(0xFFD9ECEA);
  static const Color divider = Color(0xFFE7F2F1);

  // Status
  static const Color success = Color(0xFF2E9D72);
  static const Color error = Color(0xFFD64545);
  static const Color warning = Color(0xFFF2A93B);

  // Splash screen waves
  static const Color splashWaveLight = Color(0xFFBCEDEA);
  static const Color splashWaveMedium = Color(0xFF55CEC7);
  static const Color splashWaveDark = Color(0xFF079A92);
// Home screen cards
// Home screen cards

static final Color profileCard = Colors.indigo.shade50;
static final Color medicineCard = Colors.orange.shade50;
static final Color recordsCard = Colors.teal.shade50;
static final Color qrCard = Colors.deepPurple.shade50;

// Accent colors
static const Color purple = Colors.deepPurple;
static const Color orange = Colors.orange;
static const Color green = Colors.green;
static final Color yellowLight = Colors.amber.shade100;}